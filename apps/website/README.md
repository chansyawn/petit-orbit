# Website

A minimal TanStack Start website using Vite+ and Tailwind CSS.

From the repository root:

```bash
vp run website#dev
vp run website#build
```

The home page is in `src/routes/index.tsx`. Add routes in `src/routes/`.
